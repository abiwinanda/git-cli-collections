# Get git email config (repo level)
git config --get user.email

# Set git email config (repo level)
git config user.email your@email.com

# Set git email config (global level)
git config --global user.email your@email.com
