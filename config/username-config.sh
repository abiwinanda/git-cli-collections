# Get git username config (repo level)
git config --get user.name

# Set git username config (repo level)
git config user.name ${USERNAME}

# Set git username config (global level)
git config --global user.name ${USERNAME}
